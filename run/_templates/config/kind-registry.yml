kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
containerdConfigPatches:
  - |-
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."127.0.0.1:8081"]
      endpoint = ["http://kind-registry:5000"]
nodes:
  - role: control-plane
networking:
  disableDefaultCNI: true
